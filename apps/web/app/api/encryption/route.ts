import { NextResponse } from 'next/server'

export async function POST(request: Request) {
  // Placeholder para endpoint de encriptación
  return NextResponse.json({
    message: 'Encryption endpoint - To be implemented'
  })
}
